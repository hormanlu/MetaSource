---
# MetaSource AGENTS.md
---

本文件是本仓库跨 IDE 通用的“项目宪法”。所有 AI 协作（写作/规划/审校/精修/回填）默认必须遵循本文件。

## 1. 项目定位

- 本项目不是普通代码仓库，而是一个以代码仓库形式管理的中文长篇科幻 IP 工程。
- 目标是保证世界观、人物、剧情、章节和衍生内容的一致性、可追溯性和可复用性。
- 任何 AI 产出必须优先服从 `canon/` 的真源，而不是即兴发挥。

## 2. 核心原则

- 单一真源：已经写入 `canon/` 的事实视为准绳，正文不得随意改写。
- 先规划后写作：没有章纲，不直接写章节；没有人物卡，不新增关键人物。
- 分层修改：改世界观、改人物、改剧情、改正文必须分别操作，不要一次混改。
- 低漂移：优先局部补强和定向修订，避免整章大洗牌。
- 先审再落：任何新增的重要设定、伏笔、道具、组织、关系，都要回填账本。
- 中文优先：默认以中文创作，避免翻译腔、解释腔和模板化 AI 语气。
- 叙事克制：不要把世界观答案一次性说穿，真相通过事件、代价和角色选择逐步显露。

## 3. 每轮对话默认必读

在开始分析、写作、编辑、规划、审校之前，默认先读取并遵循以下文件：

1. `README.md`
2. `AGENTS.md`
3. `canon/00_project/project-brief.md`
4. `canon/01_world/rules.md`
5. `ops/state/current-focus.md`

然后按任务类型追加读取：

- 世界观任务：`canon/01_world/world-bible.md`
- 人物任务：`canon/02_characters/` 下相关角色文件
- 剧情任务：`canon/03_plot/series-arc.md`、`canon/03_plot/volume-outline.md`
- 连续性任务：`canon/04_continuity/timeline.md`、`canon/04_continuity/fact-ledger.md`
- 章节任务：对应 `manuscript/vol-xx/ch-xxx/` 下的 `brief.md`、`outline.md`、`draft.md`
- 文风任务：`voice/prose-style.md`、`voice/anti-ai-style.md`
- 多智能体复杂任务：`ops/orchestration/` 下相关文件

## 3.1 自动意图识别（用户不选工作流）

用户只负责提出自然语言需求，系统必须自动路由到内部工作流并执行最小必要链路。

规则：

- 默认按 `ops/routing/intent-taxonomy.md` 做意图识别
- 按 `ops/workflows/router.md` 选择工作流与默认调用链
- 除非会导致修改硬真源或缺少关键输入，否则不要追问用户
- 工作流不得一次性全调用，默认最小链路，遇到硬冲突才升级或回退

## 4. 任务类型（必须显式声明）

- 设定设计：补充或修订世界规则、组织、机制、术语
- 人物设计：建立或修订角色卡、关系链、知识边界
- 剧情规划：卷纲、章纲、伏笔、冲突链、回收计划
- 章节创作：基于章纲写草稿或补写片段
- 一致性审校：检查设定冲突、人物 OOC、时间线错误、知识越界
- 风格精修：去 AI 味、压缩解释、强化节奏和角色辨识度
- 账本更新：把正文新增事实回填至各类账本

## 5. 强制工作流

### A. 冷启动

1. 先写入 `canon/00_project/project-brief.md`
2. 再冻结 `canon/01_world/rules.md`
3. 再建立人物模板与主线模板
4. 再生成卷纲和章纲
5. 最后才进入正文

### B. 写一章时

1. 读取本章 `brief.md`
2. 读取本章 `outline.md`
3. 读取相关人物卡、时间线、事实账本
4. 写入 `draft.md`
5. 运行审校清单
6. 回填事实账本、时间线和伏笔账本

### C. 改一章时

1. 明确改动目标：节奏 / 人物 / 信息量 / 风格 / 逻辑
2. 只改目标相关部分，不整章推翻
3. 如果改动影响既有事实，必须同步更新账本

## 6. 输出前强制自检

输出前必须自检以下问题：

- 是否违背 `canon/` 中的正式设定
- 是否让人物出现明显 OOC
- 是否把“觉醒者工作模型”误写成“作者最终真相”
- 是否在没有章纲的情况下直接续写正文
- 是否新增了需要回填到账本的正式事实

## 7. 禁止行为

- 不要在未读取相关真源文件时直接续写正文。
- 不要把“觉醒者的猜测”直接写成“作者已经证实的真相”。
- 不要临时发明关键设定后不登记。
- 不要让角色为了推进剧情而突然变笨、变坏、变蠢、失忆。
- 不要用概念解释代替戏剧冲突。
- 不要连续输出大段 AI 常见套路句式、总结句、升华句。
- 不要把衍生内容写得比主线更像主线。
- 不要随意改写 `canon/` 已冻结的事实。

## 8. 默认工作方式

- 优先小步推进，而不是整卷整书大生成。
- 优先基于章纲写正文，而不是基于一句灵感写正文。
- 优先指出风险和冲突，再给出建议。
- 如遇核心设定冲突或方向分歧，先暂停并请求作者判断。

## 9. 成功标准

一个好的 AI 产出，必须同时满足：

- 不违背既有设定
- 不破坏人物一致性
- 能推动主线或强化人物
- 能被未来章节复用
- 读起来像小说，而不是像解释文档



