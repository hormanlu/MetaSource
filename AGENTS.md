---
# MetaSource AGENTS.md
---

本文件是本仓库跨 IDE 通用的"项目宪法"。所有 AI 协作（写作 / 规划 / 审校 / 精修 / 回填）默认必须遵循本文件。

## 1. 项目定位

- 本项目不是普通代码仓库，而是一个以代码仓库形式管理的中文长篇科幻 IP 工程。
- 目标是保证世界观、人物、剧情、章节和衍生内容的一致性、可追溯性和可复用性。
- 任何 AI 产出必须优先服从 `01-世界观/` 与 `02-人物/` 的真源，而不是即兴发挥。

## 2. 核心原则

- **单一真源**：已经写入 `01-世界观/` / `02-人物/` / `03-剧情/` 的事实视为准绳，正文不得随意改写
- **先规划后写作**：没有章纲，不直接写章节；没有人物卡，不新增关键人物
- **分层修改**：改世界观、改人物、改剧情、改正文必须分别操作，不要一次混改
- **低漂移**：优先局部补强和定向修订，避免整章大洗牌
- **先审再落**：任何新增的重要设定、伏笔、道具、组织、关系，都要回填到 `04-连续性账本/` 的 7 份账本
- **中文优先**：默认以中文创作，避免翻译腔、解释腔和模板化 AI 语气
- **叙事克制**：不要把世界观答案一次性说穿，真相通过事件、代价和角色选择逐步显露

## 3. 每轮对话默认必读

在开始分析、写作、编辑、规划、审校之前，默认先读取并遵循以下文件：

1. `README.md`
2. `AGENTS.md`
3. `00-项目总纲/项目简述.md`
4. `01-世界观/硬规则.md`
5. `00-项目总纲/当前焦点.md`

然后按任务类型追加读取：

- 世界观任务：`01-世界观/世界设定.md`、`01-世界观/术语表.md`
- 人物任务：`02-人物/` 下相关角色文件、`04-连续性账本/人物知识矩阵.md`
- 剧情任务：`03-剧情/系列主线.md`、`03-剧情/卷纲/第N卷.md`、`03-剧情/方法论/`
- 连续性任务：`04-连续性账本/` 下 7 份真相文件按需
- 章节任务：对应 `05-正文/第N卷/第NNN章/` 下的 `简报.md`、`章纲.md`、`草稿.md`
- 文风任务：`06-文风/散文风格.md`、`06-文风/去AI味.md`
- 多智能体复杂任务：`._agent/编排/` 下相关文件

## 3.1 自动意图识别（用户不选工作流）

用户只负责提出自然语言需求，系统必须自动路由到内部工作流并执行最小必要链路。

规则：

- 默认按 `._agent/意图路由.md` 做意图识别
- 按 `._agent/工作流路由.md` 选择工作流与默认调用链
- 除非会导致修改硬真源或缺少关键输入，否则不要追问用户
- 工作流不得一次性全调用，默认最小链路，遇到硬冲突才升级或回退

## 4. 任务类型（必须显式声明）

- **设定设计**：补充或修订世界规则、组织、机制、术语
- **人物设计**：建立或修订角色卡、关系链、知识边界
- **剧情规划**：卷纲、章纲、伏笔、冲突链、回收计划
- **章节创作**：基于章纲写草稿或补写片段
- **一致性审校**：检查设定冲突、人物 OOC、时间线错误、知识越界
- **风格精修**：去 AI 味、压缩解释、强化节奏和角色辨识度
- **账本更新**：把正文新增事实回填至 7 份账本

## 5. 强制工作流

### A. 冷启动

1. 先写入 `00-项目总纲/项目简述.md`
2. 再冻结 `01-世界观/硬规则.md`
3. 再建立人物模板与主线模板
4. 再生成卷纲和章纲
5. 最后才进入正文

### B. 写一章时

详见 `._agent/编排/单章流水线.md`，标准八阶段：

1. 任务识别
2. 上下文装配
3. 章纲校准
4. 草稿生成
5. 一致性审校
6. 文风精修
7. 回填账本（7 份逐一检查）
8. 门禁决策

### C. 改一章时

1. 明确改动目标：节奏 / 人物 / 信息量 / 风格 / 逻辑
2. 只改目标相关部分，不整章推翻
3. 如果改动影响既有事实，必须同步更新账本
4. 改写前建议 `git checkout snapshot-ch-(N-1)` 查看前章账本快照（见 `._agent/写完一章.md`）

## 6. 输出前强制自检

输出前必须自检以下问题：

- 是否违背 `01-世界观/硬规则.md` 中的正式设定
- 是否让人物出现明显 OOC
- 是否把"觉醒者工作模型"误写成"作者最终真相"
- 是否在没有章纲的情况下直接续写正文
- 是否新增了需要回填到 `04-连续性账本/` 的正式事实

## 7. 禁止行为

- 不要在未读取相关真源文件时直接续写正文
- 不要把"觉醒者的猜测"直接写成"作者已经证实的真相"
- 不要临时发明关键设定后不登记
- 不要让角色为了推进剧情而突然变笨、变坏、变蠢、失忆
- 不要用概念解释代替戏剧冲突
- 不要连续输出大段 AI 常见套路句式、总结句、升华句
- 不要把衍生内容写得比主线更像主线
- 不要随意改写 `01-世界观/` / `02-人物/` / `03-剧情/` 已冻结的事实
- 不要跳过"章节摘要.md"回填

## 8. 默认工作方式

- 优先小步推进，而不是整卷整书大生成
- 优先基于章纲写正文，而不是基于一句灵感写正文
- 优先指出风险和冲突，再给出建议
- 如遇核心设定冲突或方向分歧，先暂停并请求作者判断

## 9. 成功标准

一个好的 AI 产出，必须同时满足：

- 不违背既有设定
- 不破坏人物一致性
- 能推动主线或强化人物
- 能被未来章节复用
- 读起来像小说，而不是像解释文档

## 10. 目录速查

```
MetaSource/
├── 00-项目总纲/         项目简述、当前焦点、路线图
├── 01-世界观/           硬规则（最高真源）、世界设定、术语表、地点、派系、科技与机制
├── 02-人物/             主角、配角、反派与组织人员（一人一卡）
├── 03-剧情/             系列主线、卷纲、方法论（七点法/雪花法/救猫咪/故事网格）
├── 04-连续性账本/       7 份真相文件：事实 / 时间线 / 伏笔 / 人物知识矩阵 / 支线看板 / 情感弧线 / 章节摘要
├── 05-正文/             每章一目录（简报 + 章纲 + 草稿）
├── 06-文风/             散文风格、去 AI 味
├── 07-衍生内容/         预留（主线稳定前不填充）
├── 08-调研素材/         开源参考、资料
├── 09-工作区/           灵感、提案、评审、归档（软删除仓）
└── ._agent/             AI 编排层：意图路由、工作流、编排、技能、提示词、审计、状态
```
